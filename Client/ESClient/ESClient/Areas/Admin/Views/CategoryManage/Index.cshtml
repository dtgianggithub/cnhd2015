
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div ng-app="myapp" class=" row">
    <div class="col-lg-12" ng-controller="ProductController">
        <div class="panel panel-default">
            <div class="panel-heading">
                Danh mục sản phẩm
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <span>
                            @Html.ActionLink("Thêm danh mục mới", "Create")
                        </span>
                        <button class="btn btn-success">
                            <span class="glyphicon glyphicon-user"></span>  Add User
                        </button>
                        <thead>
                            <tr>
                                <th>Mã danh mục</th>
                                <th>Tên danh mục</th>
                                <th>ten</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr ng-repeat="cata in products">
                                <td>{{cata.MA}}</td>
                                <td>{{cata.TEN}}</td>
                                <td>{{cata.DAXOA}}</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@*<script>
    var app = angular.module("ProductApp", []);
    app.controller("ProductController", function ($scope, $http) {
        $http.get('http://localhost:64896/api/sinhvien').
          success(function (data, status, headers, config) {
              $scope.products = data;

          }).
          error(function (data, status, headers, config) {
              alert("erro");
          });
    });
</script>*@




